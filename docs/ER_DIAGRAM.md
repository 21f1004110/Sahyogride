# ER Diagram

Schema and reference SQL for the concurrency-critical constraints (the `UNIQUE(seat_id)` hold constraint and the partial unique index on confirmed reservations). Filled in in SAHYOG-02.
