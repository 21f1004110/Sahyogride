# API Contract

The source of truth for every endpoint's request/response shape, status codes, and error codes. Read this before touching any endpoint or API call — see CLAUDE.md.

Filled in starting SAHYOG-03, extended as each endpoint ticket lands.
